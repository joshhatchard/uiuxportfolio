import { WorkIndexSection } from "@/components/work-index-section";

export default function WorkPage() {
  return (
    <WorkIndexSection
      eyebrow="Work"
      title="Case studies"
      description="This page is identical to the home page so the work content stays in one place."
    />
  );
}